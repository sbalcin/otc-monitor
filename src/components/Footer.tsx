export function Footer() {

    return (
        <footer className="px-5 py-2 border-t border-[var(--color-border)] flex items-center justify-between">
            <span className="text-[10px] text-[var(--color-text-muted)]">
              Internal Monitoring
            </span>
            <span className="text-[10px] text-[var(--color-text-muted)]">
              Data: Binance, Bitstamp, Mock Positions
            </span>
        </footer>
    )
}